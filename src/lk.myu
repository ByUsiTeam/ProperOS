fn lock(data)

uigo("lock.iyu")
sss lock_ui =data

end fn

fn y()

fe("$System/Config/Pwd/sys",pwd)
fe("$System/Config/Pwd/pwd_lock",pwd_lock)
f(pwd == true)
{
	fn lk.lock(pwd)
}
else
{
	f(pwd_lock == true)
	{
		syso("[Pwd_Lock] true")
	}
    else
    {
    	fn lk.lock(pwd)
    }
}

end fn