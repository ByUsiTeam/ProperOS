import android.webkit.WebView;
public setUa(WebView wv,String ua_){
	String ua = wv.getSettings().getUserAgentString();
	wv.getSettings().setUserAgentString(ua_);
}

public static String getUa(WebView wv){
	String ua = wv.getSettings().getUserAgentString();
	return ua;
}